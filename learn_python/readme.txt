1、reload时需要使用括号，并且要记住让代码的最新版本运行时首先要使用reload
2、reload是不可传递的，重载一个模块的话只会重载该模块，而不能够重载该模块所导入的任何模块
3、避免使用import和reload启动程序，如果必须导入，为避免复杂性，请将所有文件放在同一个目录下
4、基本的import语句每个进程只会运行文件一次
5、S[-1]反向索引，S[I:J]表示从I开始直到J结束，不包括J
6、Python的字符串具有不可变性，即创建后不能就地改变
7、python核心数据类型可以支持任意的嵌套